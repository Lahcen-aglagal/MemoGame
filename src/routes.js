import { Home } from './Pages'

export const routes = [
    {
        name: 'Home',
        key: 'home',
        path: '/', 
        component: Home, 
        exact: true,
        route : 'home'
    }
]